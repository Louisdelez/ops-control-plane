# Atlas may request exactly one bounded local credential reload after OpenBao
# has accepted and durably stored a provider key.  The root helper accepts no
# arguments and cannot read, print, delete, or rotate any credential.
ops-user ALL=(root) NOPASSWD: /usr/local/libexec/ops-model-credentials-reload
